package various;

import org.junit.jupiter.api.Test;
import java.util.Stack;
import java.util.TreeMap;
/*

 Problem
     Design a max stack data structure that supports push, pop, top, peekMax and popMax.
     push(x) -- Push element x onto stack.
     pop() -- Remove the element on top of the stack and return it.
     top() -- Get the element on the top.
     peekMax() -- Retrieve the maximum element in the stack.
     popMax() -- Retrieve the maximum element in the stack, and remove it. If you find more than one maximum element, only remove the top-most one.
    heap with linked list

     */
final class StackScenarios {

  @Test
  void push_on_the_stack(){
      // Given
    Stack stack = new Stack();


      // When

      // Then
  }
}
